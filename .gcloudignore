# Git
.git/
.gitignore

# Rust build artifacts
target/

# Claude / dev tooling
.claude/
.cargo/

# Runtime config (secrets — never needed in Docker build context)
config.toml
*.config.toml
.env
.env.*

# Docs and assets that aren't needed for the Docker build
docs/

# OS
.DS_Store
